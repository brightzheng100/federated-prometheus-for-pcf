# if one wants to have a centralized grafana only to visualize metrics, remove it from "agent" Prometheus

- type: remove
  path: /instance_groups/name=nginx

- type: remove
  path: /variables/name=alertmanager_password?
- type: remove
  path: /variables/name=prometheus_password?
